#include <iostream>

using namespace std;

int main(){
    //En formato completo: 
    // + suma
    // - resta
    // * multiplicacion
    // / division entera por default. Para ver la division entera hay que hacer la variable de tipo flotante
    // % resto de division
    //En formato reducido se usa +=, -=, *=, /= y %= según corresponda.
    //Para incrementar o decrementar en una unidad se usa ++ o --  según corresponda. 
    
    return 0;
}