#include <iostream>

using namespace std;

int main(){
    // || es para 'or'
    // && es para 'and'
    // ! es para 'not'
    
    // < menor
    // > mayor
    // <= menor o igual
    // >= mayor o igual
    // == igual
    // != distinto 
    
    return 0;
}